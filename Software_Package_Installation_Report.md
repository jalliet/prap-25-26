# Software Package Installation List
---

## 1. Installed Packages List
* **catkin_pkg**
* **empy**
* **lark**
* **casadi**
* **ros-jazzy-ros-ign-ros2-control**
* **ros-jazzy-gz-ros2-control**
* **libxcb-cursor0**

---

## 2. Execution Commands
### Python Environment and System Environment
(These are the command records I used during the download process)
```bash
pip install -U catkin_pkg
pip install -U empy
pip install -U lark
sudo apt install python3-casadi
sudo apt install ros-jazzy-ros-ign-ros2-control
sudo apt install ros-jazzy-gz-ros2-control
sudo apt install libxcb-cursor0